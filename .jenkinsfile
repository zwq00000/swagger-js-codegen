pipeline {
    agent { docker 'node:latest' }
    stages {
        stage('build') {
            steps {
                sh 'users'
                sh 'ls -l'
                sh 'npm install'
                sh 'npm run build'
                sh 'npm run test'
            }
        }
    }
}