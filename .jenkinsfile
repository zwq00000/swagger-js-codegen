pipeline {
    agent { 
            docker 'node:latest' 
            args  '-v /tmp:/.npm' 
    }
    stages {
        stage('build') {
            steps {
                sh 'npm install'
                sh 'npm run build'
                sh 'npm run test'
            }
        }
    }
}