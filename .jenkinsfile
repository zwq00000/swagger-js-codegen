pipeline {
    agent { docker 'node:6.3' }
    stages {
        stage('build') {
            steps {
                sh 'users'
                sh 'ls -l'
                sh 'npm install'
                sh 'npm run build'
                sh 'npm run test'
            }
        }
    }
}