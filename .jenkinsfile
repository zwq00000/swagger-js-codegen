pipeline {
    agent { docker 'node:least' }
    stages {
        stage('build') {
            steps {
                sh 'users'
                sh 'ls -l'
                sh 'npm install'
                sh 'npm run build'
                sh 'npm run test'
            }
        }
    }
}